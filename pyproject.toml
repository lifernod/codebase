[project]
name = "codebase"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
    "python-multipart>=0.0.32",
    "tabulate>=0.10.0",
    "uvicorn>=0.49.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
